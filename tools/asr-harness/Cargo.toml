[package]
name = "asr-harness"
version = "0.1.0"
edition = "2021"

# Standalone crate — do not join the meetily workspace
[workspace]

[dependencies]
whisper-rs = { version = "0.13.2", features = ["raw-api", "metal"] }
hound = "3.5"
anyhow = "1.0"

[profile.release]
opt-level = 3
